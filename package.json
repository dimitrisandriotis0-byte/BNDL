{
  "name": "bndl-website",
  "version": "1.0.0",
  "description": "Premium E-commerce Website for BNDL",
  "main": "index.html",
  "scripts": {
    "dev": "browser-sync start --server --files '*.html, css/*.css, js/*.js' --port 3000",
    "start": "npx serve . -p 3000"
  },
  "devDependencies": {
    "browser-sync": "^3.0.3"
  }
}
